{
  "scripts": {
    "clean": "rimraf bin cache obj output temp wwwroot",
    "build": "ts-node ./src/build.ts",
    "format:fix": "prettier --write ./**/*.{css,md,json,scss,ts}"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2"
  }
}
